# Generated outputs

`pulp_cbc/` and `pyomo_gurobi/` contain the current figures and Excel workbooks generated from the corresponding implementation.  The plotted dispatches can differ between solvers because a linear dispatch model can have multiple equally optimal hourly flow patterns.

`legacy_pulp/` preserves the earlier repository outputs for traceability and is not the current reporting baseline.
